# Commun
server.error.include-message=always
server.error.include-binding-errors=always
# Cloud Run fournit le port via PORT
server.port=${PORT:8080}
# CORS - Production: padelrounds.com | Fallback: nextapp | Dev: localhost
app.cors.allowed-origins=https://padelrounds.com,https://www.padelrounds.com,https://nextapp-tcepdy5iwa-uc.a.run.app,http://localhost:3000,http://192.168.1.12:3000
logging.level.org.springframework.security=INFO
spring.jpa.properties.hibernate.default_batch_fetch_size=50
# Flyway - desactive par defaut, active par profil
spring.flyway.enabled=false