# ========================================
# Configuration de la base de données PostgreSQL locale
# ========================================
# IMPORTANT : Copier ce fichier vers .env.local et remplir avec vos vraies valeurs
# Le fichier .env.local ne sera JAMAIS commité sur Git

# Utilisateur de la base de données
DB_USER=votre_utilisateur

# Mot de passe de la base de données
DB_PASSWORD=votre_mot_de_passe

# Nom de la base de données
DB_NAME=padel_db

# Hôte de la base de données (pour prod)
DB_HOST=localhost

# Port de la base de données (pour prod)
DB_PORT=5432

# ========================================
# Instructions
# ========================================
# 1. Copier ce fichier : cp .env.example .env.local
# 2. Remplir les vraies valeurs dans .env.local
# 3. JAMAIS commiter .env.local sur Git
# 4. Pour charger les variables : export $(grep -v '^#' .env.local | xargs)

